import { Area, Restaurant } from '@modules';
// Domain service example (not an application service)
export class CapacityCalculationService {
  calculateMaximumCapacity(restaurant: Restaurant, areas: Area[]): number {
    // Lógica compleja para calcular la capacidad máxima
    // basada en las propiedades del restaurante y sus áreas
    return 0;
  }

  // Otros métodos relacionados con el cálculo de capacidades
}
